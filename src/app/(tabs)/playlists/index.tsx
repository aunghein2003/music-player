import { Text, View } from "react-native";
import { defaultStyles } from "@/styles";

export default function PlaylistsScreen() {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>This is playlists</Text>
    </View>
  );
}
