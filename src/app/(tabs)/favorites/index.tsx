import { Text, View } from "react-native";
import { defaultStyles } from "@/styles";

export default function FavoritesScreen() {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>This is Favorite Screen</Text>
    </View>
  );
}
